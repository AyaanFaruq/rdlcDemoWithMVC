@model IEnumerable<rdlcDemo.Models.Employee>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create") |
    @Html.ActionLink(linkText: "Show Salary", actionName: "ShowAll", controllerName: "Employees", routeValues: null, htmlAttributes: new { target = "_blank" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Address)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |           
         
            @Html.ActionLink("Show", "Show", new { id = item.Id }) 

            @*@Html.ActionLink(linkText: "Show", actionName: "Show", controllerName: "Employees", routeValues: null, htmlAttributes: new { target = "_blank" })*@
         
        </td>
    </tr>
}

</table>
